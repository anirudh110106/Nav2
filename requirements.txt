# Python dependencies for LeKiwi Nav2 stack
# Install with: pip install -r requirements.txt
# Note: ROS2 core + Nav2 + slam_toolbox are NOT pip packages — see dependencies.md

numpy>=1.24
transforms3d>=0.4.1
pyyaml>=6.0
setuptools>=68.0